apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: bpfman
  namespace: kube-system
  labels:
    app: bpfman
spec:
  selector:
    matchLabels:
      app: bpfman
  template:
    metadata:
      labels:
        app: bpfman
    spec:
      containers:
        - name: bpfman
          image: bpfman:dev
          imagePullPolicy: IfNotPresent
          args: ["serve"]
          securityContext:
            privileged: true
          volumeMounts:
            - name: bpffs
              mountPath: /sys/fs/bpf
              mountPropagation: Bidirectional
            - name: bpfman-sock
              mountPath: /run/bpfman-sock
      volumes:
        - name: bpffs
          hostPath:
            path: /sys/fs/bpf
            type: DirectoryOrCreate
        - name: bpfman-sock
          hostPath:
            path: /run/bpfman-sock
            type: DirectoryOrCreate
      tolerations:
        - operator: Exists
